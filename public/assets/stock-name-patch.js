document.addEventListener("DOMContentLoaded",async()=>{await DFBNames.init();const patch=()=>{document.querySelectorAll("h3,.flow-row strong,.research-card .eyebrow,.sector-card .eyebrow,td").forEach(el=>{if(el.dataset.namePatched)return;const t=el.textContent.trim();const m=t.match(/^(?:TWSE|TPEX)?\s*(\d{4,6})$/);if(m){const n=DFBNames.name(m[1]);if(n!==m[1]){el.textContent=`${n}（${m[1]}）`;el.dataset.namePatched="1"}}})};patch();new MutationObserver(patch).observe(document.body,{childList:true,subtree:true})});